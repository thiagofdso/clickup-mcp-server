apiVersion: v1
kind: Service
metadata:
  name: clickup-mcp-http
  labels:
    app: clickup-mcp
spec:
  type: NodePort
  selector:
    app: clickup-mcp
  ports:
    - name: http
      protocol: TCP
      port: 3231
      targetPort: 3231
      nodePort: 30231
